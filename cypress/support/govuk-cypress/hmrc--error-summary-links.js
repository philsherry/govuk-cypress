/* eslint-disable no-unused-vars */
/// <reference types="cypress" />
/**
 * @author:
 * @function checkErrorSummaryLinks
 * @description Cypress function to CHECK THE THING DOES THE THING.
 * @example cy.get("@component").checkErrorSummaryLinks();
 * @link
 **/

// -- This is a child command --
Cypress.Commands.add(
  'checkErrorSummaryLinks',
  {
    prevSubject: 'element',
  },
  (subject, options) => {
    //
  }
);

// Does this functionality already exist in .checkGovukErrorSummary()?
